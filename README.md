# Project1

A minimal Vite + TypeScript + React starter.

Run locally:

```bash
npm install
npm run dev
```

Typecheck:

```bash
npm run typecheck
```

Build:

```bash
npm run build
npm run preview
```
# Project1
